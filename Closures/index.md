# Closure #

```
Js  has a lexical scope environment. If a function need to access the variables  first it will check the local memory if we didn't find the variable then it goes the the lexical parent.

Innerfunction can directly access the variables from the outerfunction.

```