# setTimeOut #

```
It is called after specific time of interval.
setTimeout takes a callback function and attaches a timer and stores it.
After specific time interval js takes a function, puts it into a call stack and runs it.
```